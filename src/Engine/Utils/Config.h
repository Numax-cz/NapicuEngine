#pragma once

namespace Napicu{
    struct Config{
        inline const static int GRID_WIDTH = 32;
        inline const static int GRID_HEIGHT = 32;

        inline const static int CAMERA_PROJECTION_SIZE_X = 1920;
        inline const static int CAMERA_PROJECTION_SIZE_Y = 1080;

    };
}